// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("exdef", "for exdef support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("exdef", "enable exdef support", "no");

if (PHP_EXDEF != "no") {
	EXTENSION("exdef", "exdef.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

